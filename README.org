#+begin_src plantuml :file /ssh:gc-sing:/home/larva/.emacs.d/private/david/_assets/img/loading.svg :dir /ssh:gc-sing:/home/larva/.emacs.d/private/david/
    'loading in spacemacs layer :exports results
skinparam maxMessageSize 40
    layers.el --> packages.el :layer.el always\n loading first
    packages.el --> funcs.el : funcs.el always secure\n weth when package enabled
  '  packages.el --> keybindings.el : 
    funcs.el --> config.el :config.el should only\n put setq and defvar
    config.el --> keybindings.el
    keybindings.el --> .spacemacs
      funcs.el --> keybindings.el
    note over "packages.el", config.el: not sure which file\n will load first.\n that why always wrapping them with secure\n function like init, preinit,postinit\n
#+end_src

#+RESULTS:
[[file:/ssh:gc-sing:/home/larva/.emacs.d/private/david/_assets/img/loading.svg]]
